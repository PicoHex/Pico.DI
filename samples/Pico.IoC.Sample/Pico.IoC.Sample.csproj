<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>

        <LangVersion>latest</LangVersion>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>

        <PublishAOT>true</PublishAOT>
        <StripSymbols>false</StripSymbols>
        <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    </PropertyGroup>

    <ItemGroup>
      <ProjectReference Include="..\..\src\Pico.IoC.Abs\Pico.IoC.Abs.csproj" />
      <ProjectReference Include="..\..\src\Pico.IoC\Pico.IoC.csproj" />
      <ProjectReference Include="..\Pico.IoC.Sample.Services\Pico.IoC.Sample.Services.csproj" />
    </ItemGroup>

    <!-- Source Generator: use ProjectReference only when NOT trimming/publishing -->
    <ItemGroup Condition="'$(PublishTrimmed)' != 'true'">
      <ProjectReference Include="..\..\src\Pico.IoC.Gen\Pico.IoC.Gen.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
    </ItemGroup>

    <!-- When trimming/publishing, reference the pre-built analyzer DLL -->
    <ItemGroup Condition="'$(PublishTrimmed)' == 'true'">
      <Analyzer Include="..\..\src\Pico.IoC.Gen\bin\Debug\netstandard2.0\Pico.IoC.Gen.dll" />
    </ItemGroup>

</Project>
